# Pick Report

S-101 has the idea of a "pick report" which is what shows when a human makes a chart query (usually clicking the screen). Since we want to remove all of the computation from the client, the data could actually be converted into the pick report symbolization by the server.
